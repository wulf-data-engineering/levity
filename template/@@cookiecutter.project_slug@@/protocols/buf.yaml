version: v1
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX   # Allows "package foo" instead of "package foo.v1"
breaking:
  use:
    - WIRE # allow renaming which breaks json but not protocol buffers
